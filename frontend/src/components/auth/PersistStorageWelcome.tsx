// Placeholder — Plan 03 ships full PersistStorageWelcome (D-15 navigator.storage.persist flow).
export function PersistStorageWelcome() {
  return (
    <main className="flex min-h-dvh items-center justify-center p-6">
      <p className="text-[var(--color-text-muted)] text-[var(--text-base)]">
        Welcome — placeholder. Plan 03 lands persistent-storage welcome.
      </p>
    </main>
  );
}
