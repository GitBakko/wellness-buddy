// Placeholder — Plan 07 ships /today (greeting, MealCard, MacroDisplay, WeightQuickLog,
// WorkoutForm, DayStatusIndicator, AIWidgetPlaceholder).
export default function Today() {
  return (
    <main className="flex min-h-dvh items-center justify-center p-6">
      <p className="text-[var(--color-text-muted)] text-[var(--text-base)]">
        Oggi — placeholder. Plan 07 lands real /today.
      </p>
    </main>
  );
}
