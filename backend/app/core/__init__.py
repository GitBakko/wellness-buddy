"""Core scaffolding (config + deps).

NOTE (worktree race): Plan 02a is the canonical owner of `app/core/` — this
file is a minimal stub created by Plan 02b because 02a had not yet landed in
this worktree at execution time. Wave 2 merge will reconcile.
"""
