---
phase: 02-differentiators
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md
  - .planning/phases/01-foundation/01-08-tone-calibration-checklist.md
  - frontend/src/i18n/copy.it.ts
autonomous: false
requirements:
  - DEP-06
  - UI-09
  - UI-11
  - UI-13
  - UI-17
  - UI-19
tags:
  - phase-2
  - deploy
  - production
  - iphone-install
  - lighthouse
  - tone-calibration
  - human-verify
  - checkpoint
must_haves:
  truths:
    - "Stefano ran DEPLOY.md end-to-end on Windows Server 2019 production host (wellness-buddy.epartner.it)"
    - "Stefano installed PWA on his iPhone Safari (Share → Aggiungi a Home)"
    - "Marta installed PWA on her iPhone Safari"
    - "/today renders Lifesum Pure theme on both iPhones in standalone display mode"
    - "Offline /today works (airplane mode → Dexie cache → reconnect → sync ≤5s)"
    - "Lighthouse PWA score ≥95 measured on https://wellness-buddy.epartner.it/today"
    - "Lighthouse Accessibility score ≥95 on same URL"
    - "Stefano + Marta both filled the 02-03-DEPLOY-CHECKLIST.md sign-off table with PASS verdict on all 12 sections"
    - "Tone calibration variant A · Lifesum Pure locked (Phase 1 deferred sign-off ritual closed mid-Phase-2)"
    - "PWA install follow-up copy keys added to copy.it.ts (D-26)"
  artifacts:
    - path: ".planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md"
      provides: "12-section deploy checklist artifact with Stefano + Marta sign-off table"
      contains: "## 1. Pre-flight — DNS + firewall"
      contains2: "## 12. Sign-off"
      contains3: "Stefano"
      contains4: "Marta"
    - path: ".planning/phases/01-foundation/01-08-tone-calibration-checklist.md"
      provides: "Variant A · Lifesum Pure final sign-off (deferred T3 closure absorbed into Plan 02-03)"
      contains: "Variant A"
      contains2: "Lifesum Pure"
    - path: "frontend/src/i18n/copy.it.ts"
      provides: "pwa.installFollowUp* keys (3 leaves) for post-deploy iPhone install help"
      contains: "installFollowUpHeading"
      contains2: "installFollowUpBody"
  key_links:
    - from: "02-03-DEPLOY-CHECKLIST.md §11 Tone calibration sign-off"
      to: "01-08-tone-calibration-checklist.md Variant A locked entry"
      via: "Cross-reference closing the deferred Phase 1 T3 ritual"
      pattern: "Variant A.*Lifesum Pure"
    - from: "02-03-DEPLOY-CHECKLIST.md §10 Lighthouse audit"
      to: "frontend/lighthouserc.json thresholds (Phase 1 Plan 05b — pwa ≥95, a11y ≥95)"
      via: "Manual measurement against committed thresholds"
      pattern: "pwa.*95"
---

<objective>
Production deploy CHECKPOINT (D-26..D-28). Stefano executes DEPLOY.md end-to-end on Windows Server 2019 (`wellness-buddy.epartner.it`); Stefano + Marta install the PWA on their iPhones from the production HTTPS URL; both validate offline `/today` + variant selector (shipped Plan 02-02) + Lifesum Pure rendering; Lighthouse PWA + a11y ≥95 measured; tone calibration variant A locked (closing deferred Phase 1 T3 sign-off). This plan IS the Phase 1 pause-gate gate, absorbed mid-Phase-2 per project owner decision.

Purpose: Phase 2 cannot ship Plan 02-04 (shopping list — depends on cross-device sync verification) or Plan 02-05 (PDF export — depends on production WeasyPrint stability) without proving the production deploy works on real iPhones with real users. This plan delivers the empirical foundation.

Output: Signed `02-03-DEPLOY-CHECKLIST.md` with all 12 sections marked PASS by both Stefano + Marta. Updated `01-08-tone-calibration-checklist.md` reflecting variant A · Lifesum Pure locked. New PWA install follow-up copy keys for post-install help banner.

**This plan has a `checkpoint:human-verify` blocking gate. Plan 02-04, 02-05, 02-06, 02-07 cannot start until this checkpoint resolves PASS from BOTH reviewers.**
</objective>

<execution_context>
@C:/Users/bakko/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/bakko/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-differentiators/02-CONTEXT.md
@.planning/phases/02-differentiators/02-UI-SPEC.md
@.planning/phases/01-foundation/VERIFICATION.md
@.planning/phases/01-foundation/01-08-tone-calibration-checklist.md
@DEPLOY.md
@deploy/nssm/install-service.ps1
@deploy/iis/web.config
@deploy/scripts/generate-secrets.ps1
@deploy/scripts/smoke-test.ps1
@deploy/win-acme/README.md
@frontend/lighthouserc.json
@mockups/tone-calibration-v2/A-lifesum-pure.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 02-03-DEPLOY-CHECKLIST.md artifact + extend copy.it.ts pwa.* with install follow-up keys</name>
  <files>.planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md, frontend/src/i18n/copy.it.ts</files>
  <read_first>
    - .planning/phases/02-differentiators/02-UI-SPEC.md (lines 437-534 — §6.6 Plan 02-03 deploy checklist anatomy verbatim — 12 sections)
    - .planning/phases/01-foundation/01-08-tone-calibration-checklist.md (analog sign-off table shape)
    - DEPLOY.md (existing 16-section walkthrough — checklist references it)
    - frontend/src/i18n/copy.it.ts (existing pwa.* keys to preserve while extending)
  </read_first>
  <behavior>
    - Test 1: `02-03-DEPLOY-CHECKLIST.md` exists with EXACTLY 12 numbered sections (§1 Pre-flight → §12 Sign-off)
    - Test 2: §12 Sign-off table has 2 reviewer rows (Stefano + Marta) with columns: Reviewer, Initial, Date, Time, Verdict (PASS/CONCERNS/BLOCK), Notes
    - Test 3: §10 Lighthouse audit has 4 score lines (PWA ≥95, A11y ≥95, Performance informational, Best Practices informational)
    - Test 4: §11 Tone calibration cross-references `mockups/tone-calibration-v2/A-lifesum-pure.html` and lists 5 sub-items
    - Test 5: copy.it.ts gains `pwa.installFollowUpHeading`, `pwa.installFollowUpBody`, `pwa.installFollowUpDismiss` (3 new leaves) within the existing pwa.* namespace
  </behavior>
  <action>
    1. **Create `.planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md`** with EXACT content from UI-SPEC §6.6 (lines 444-526 verbatim — 12 sections + sign-off table). Use the markdown verbatim:

    ```markdown
    # Plan 02-03 — Production Deploy CHECKPOINT (Stefano + Marta)

    **Date attempted:** ____________  
    **Server:** Windows Server 2019 · `wellness-buddy.epartner.it`  
    **Deploy script source:** `DEPLOY.md` (Plan 01-08 deliverable + Plan 02-01 Appendix B)

    ## 1. Pre-flight — DNS + firewall
    - [ ] DNS A record `wellness-buddy.epartner.it` → server IP (verify via `nslookup wellness-buddy.epartner.it`)
    - [ ] Firewall allows 80/443 inbound (verify via `Test-NetConnection -ComputerName wellness-buddy.epartner.it -Port 443`)
    - [ ] PostgreSQL service running (`Get-Service postgresql-x64-*`)

    ## 2. Database
    - [ ] CREATE DATABASE `wellness_buddy_prod` OWNER `wellness_buddy`
    - [ ] `uv sync --frozen` in backend dir (pulls weasyprint + reportlab + apscheduler from Plan 02-01)
    - [ ] `alembic upgrade head` returns no errors (`a694bcd4d792` Phase 1 baseline; Plan 02-06 ships `0001_activate_groups`)
    - [ ] `\dt` in psql shows expected 12+ tables (users, groups, refresh_tokens, invite_tokens, nutrition_plans, weekly_plan_variants, weight_logs, workout_logs, shopping_list_states, ai_event_log, audit_log, alembic_version)

    ## 3. Secrets
    - [ ] `pwsh deploy/scripts/generate-secrets.ps1` produces `.env.production`
    - [ ] JWT secret rotated (≠ dev value; ≥32 random bytes)
    - [ ] DATABASE_URL points to `wellness_buddy_prod`
    - [ ] CORS_ORIGINS = `https://wellness-buddy.epartner.it`
    - [ ] PDF_BACKEND=weasyprint (Plan 02-01 default; Stefano flips to reportlab if 7-day spike fails)

    ## 4. NSSM service
    - [ ] `nssm install WellnessBuddyAPI` succeeded (script: `deploy/nssm/install-service.ps1`)
    - [ ] Service auto-start enabled
    - [ ] Service starts cleanly (`Get-Service WellnessBuddyAPI` → Running)
    - [ ] Application Event Log shows uvicorn startup, no errors
    - [ ] `C:\msys64\mingw64\bin` present in System PATH AND inherited by NSSM (`nssm get WellnessBuddyAPI AppEnvironmentExtra` lists it OR System PATH propagated post reboot)

    ## 5. IIS reverse proxy
    - [ ] `web.config` deployed at IIS site root (file: `deploy/iis/web.config`)
    - [ ] URL Rewrite + ARR modules confirmed installed (`Get-WindowsFeature` + IIS GUI)
    - [ ] HTTPS smoke test 200 from local → reverse-proxy → backend (`Invoke-WebRequest https://wellness-buddy.epartner.it/api/health`)
    - [ ] WebSocket upgrade headers preserved (`curl -i -H "Connection: Upgrade" -H "Upgrade: websocket" ...` returns 101 or 200 with no rewrite stripping)

    ## 6. SSL via win-acme
    - [ ] `wacs.exe` interactive run completed for `wellness-buddy.epartner.it` (script: `deploy/win-acme/README.md`)
    - [ ] Certificate issued + auto-renewal scheduled task created (`Get-ScheduledTask | Where-Object { $_.TaskName -match 'win-acme' }`)
    - [ ] `https://wellness-buddy.epartner.it` returns 200 from EXTERNAL connection (mobile network, NOT same LAN)
    - [ ] Browser shows "Connessione protetta" (no cert warnings, no mixed-content)

    ## 7. Smoke test
    - [ ] `pwsh deploy/scripts/smoke-test.ps1` → all checks green
    - [ ] `/api/health` returns `{"status": "ok"}`
    - [ ] `/api/version` returns expected git sha (matches `git rev-parse HEAD` on deploy host)
    - [ ] First user login succeeds end-to-end (Stefano account)
    - [ ] /api/weekly/{week_start} returns 200 for Stefano's seeded plan (Plan 02-02 endpoint live)

    ## 8. iPhone install (Stefano)
    - [ ] Open Safari → `https://wellness-buddy.epartner.it`
    - [ ] Tap Share → "Aggiungi a Home" visible in share menu
    - [ ] Add to home screen — custom icon visible (NOT generic web clip; FND-05 + UI-09 manifest icons 192/512 + theme color)
    - [ ] Tap home-screen icon → app opens in standalone mode (no Safari chrome, no URL bar)
    - [ ] /today renders with Lifesum Pure theme (warm cream bg, MacroRing centered, Plus Jakarta font, leaf-sage primary CTA, Geist Mono numerics, Instrument Serif greeting)
    - [ ] /settimana/{thisWeek} renders (Plan 02-02 ship validation) — week picker + variant pills visible
    - [ ] Toggle airplane mode ON → reload /today → cached page renders + sync pip shows "Offline"
    - [ ] Toggle airplane mode OFF → sync pip flips to "Sincronizzato" within 5s (TanStack Query refetchOnReconnect)
    - [ ] Kill app via swipe-up → reopen → still logged in (no logout storm — refresh token rotation working, AUTH-08 grace window)

    ## 9. iPhone install (Marta)
    - [ ] Same 9 steps as §8, with Marta's account on Marta's iPhone
    - [ ] Verify Marta sees ONLY her plan / weight / workout (NOT Stefano's data)
    - [ ] (FAM-* not yet shipped — Plan 02-06 will validate cross-user) — for Plan 02-03 only verify Marta's data isolation

    ## 10. Lighthouse audit (Stefano, Chrome desktop pointing at production URL `https://wellness-buddy.epartner.it/today`)
    - [ ] Lighthouse PWA score: ____ / 100 (target ≥95 per D-28 — record actual; lock 100/100 if achievable)
    - [ ] Lighthouse Accessibility score: ____ / 100 (target ≥95 per D-28)
    - [ ] Lighthouse Performance score: ____ / 100 (informational — Phase 4 hardens)
    - [ ] Lighthouse Best Practices score: ____ / 100 (informational)

    ## 11. Tone calibration sign-off (Stefano + Marta — IN PERSON, both iPhones present)
    - [ ] Both users open /today on their respective iPhones (production install from §8 / §9)
    - [ ] Confirm Lifesum Pure variant A rendering matches `mockups/tone-calibration-v2/A-lifesum-pure.html` expectation
    - [ ] Confirm fonts (Plus Jakarta + Geist Mono + Instrument Serif greeting) load correctly
    - [ ] Confirm dark mode toggle works (Settings → Tema → Scuro)
    - [ ] No "office app" tone drift observed
    - [ ] No infantile mascots or `!` in error copy spotted (UI-17 inheritance)
    - [ ] Variante A · Lifesum Pure: LOCKED (cross-reference `01-08-tone-calibration-checklist.md` final sign-off)

    ## 12. Sign-off

    | Reviewer | Initial | Date (YYYY-MM-DD) | Time (HH:MM) | Verdict (PASS/CONCERNS/BLOCK) | Notes |
    |----------|---------|-------------------|--------------|-------------------------------|-------|
    | Stefano  |         |                   |              |                               |       |
    | Marta    |         |                   |              |                               |       |

    **Closure rule (D-26):** Any `BLOCK` verdict from either reviewer keeps this checkpoint non-merged → blocks Phase 2 progression to Plan 02-04. `CONCERNS` notes must be triaged into Plan 02-07 closure tasks. Both reviewers must mark `PASS` for the checkpoint to clear.
    ```

    2. **Extend `frontend/src/i18n/copy.it.ts`** — find the existing `pwa.*` namespace (Phase 1) and add the 3 new keys per UI-SPEC §7.1 lines 660-668, preserving Phase 1 keys:

    ```typescript
      // ───── PWA install follow-up (D-26 — post-deploy iPhone install help) ─────
      // Add INSIDE existing pwa namespace — do NOT replace existing Phase 1 keys
      pwa: {
        // ... existing Phase 1 keys preserved ...
        installFollowUpHeading: 'Installazione completata',
        installFollowUpBody: 'Ora puoi aprire Wellness Buddy dalla schermata Home come app.',
        installFollowUpDismiss: 'Ho capito',
      },
    ```

    3. Run `cd frontend && pnpm typecheck && pnpm lint` to confirm copy.it.ts edits are clean.

    4. Commit: `docs(02-03): production deploy CHECKPOINT checklist + pwa install follow-up copy (D-26..D-28)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md && grep -c "^## " .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md && grep -c "Stefano\|Marta" .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md && grep -c "installFollowUpHeading\|installFollowUpBody\|installFollowUpDismiss" frontend/src/i18n/copy.it.ts && cd frontend && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md` exits 0
    - `grep -c "^## [0-9]" .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md` returns ≥12 (12 numbered sections)
    - `grep -c "^| Stefano\|^| Marta" .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md` returns ≥2 (sign-off table rows)
    - `grep -c "Lighthouse PWA score\|Lighthouse Accessibility score" .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md` returns ≥2
    - `grep -c "installFollowUp" frontend/src/i18n/copy.it.ts` returns ≥3
    - `cd frontend && pnpm typecheck` returns clean exit 0
  </acceptance_criteria>
  <done>
    Deploy checklist artifact committed; 3 new pwa.* copy keys added (preserving Phase 1 keys); Italian copy compile passes typecheck. Stefano can now run the checklist; this is the artifact he fills in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — Stefano + Marta production deploy + iPhone install + tone calibration sign-off</name>
  <what-built>
    Plan 02-01 shipped WeasyPrint dependencies + GTK3 install instructions (Appendix B in DEPLOY.md). Plan 02-02 shipped /settimana route + variant selector + LWW conflict UX. Task 1 of this plan created the `02-03-DEPLOY-CHECKLIST.md` artifact with 12 sections.

    Now Stefano executes the deploy on Windows Server 2019 production host, both Stefano AND Marta install the PWA on their iPhones, both confirm Lifesum Pure tone calibration, and both fill the sign-off table.
  </what-built>
  <how-to-verify>
    **STEFANO — execute on Windows Server 2019 production host:**

    1. **Pull latest code** to deploy host:
       ```powershell
       cd D:\Develop\AI\WellnessBuddy
       git pull origin master
       ```

    2. **Open `02-03-DEPLOY-CHECKLIST.md`** in editor — Stefano works through it linearly, checking each box as it passes.

    3. **§1-§7: Server-side deploy:**
       - Follow `DEPLOY.md` sections 1-15 verbatim (Phase 1 walkthrough)
       - Follow `DEPLOY.md` Appendix B (Plan 02-01) for MSYS2 + Pango install + PATH update
       - Run `deploy/scripts/generate-secrets.ps1` for `.env.production`
       - Run `deploy/nssm/install-service.ps1` to install Windows Service
       - Deploy `deploy/iis/web.config` to IIS site root + verify URL Rewrite + ARR modules
       - Run `wacs.exe` for Let's Encrypt cert per `deploy/win-acme/README.md`
       - Run `deploy/scripts/smoke-test.ps1` — all checks green
       - Verify `https://wellness-buddy.epartner.it/api/health` returns 200 from EXTERNAL connection (test on mobile cellular, NOT LAN)

    4. **§8: iPhone install (Stefano):**
       - Open Safari on iPhone → `https://wellness-buddy.epartner.it`
       - Login with Stefano account
       - Tap Share → "Aggiungi a Home" → confirm icon installs
       - Tap home-screen icon → app opens standalone (no Safari chrome)
       - Verify `/today` renders Lifesum Pure: warm cream bg, MacroRing center, Plus Jakarta + Geist Mono + Instrument Serif greeting, leaf-sage primary CTA
       - Verify `/settimana/{thisWeek}` (Plan 02-02 ship): WeekPicker chip row + WeeklyMacroRing + 7 day sections + variant pills
       - Toggle airplane mode → reload `/today` → cached page renders, sync pip "Offline"
       - Disable airplane mode → wait ≤5s → sync pip "Sincronizzato"
       - Kill app via swipe-up → reopen → still logged in (refresh rotation works)

    5. **§10: Lighthouse audit (Stefano, Chrome desktop):**
       - Chrome → DevTools → Lighthouse → run on `https://wellness-buddy.epartner.it/today`
       - Record PWA score (must be ≥95 per D-28; aim 100/100)
       - Record Accessibility score (must be ≥95)
       - Record Performance + Best Practices (informational only — Phase 4 hardens)
       - Fill scores in §10 of checklist

    **MARTA — execute on her iPhone:**

    6. **§9: iPhone install (Marta):**
       - Repeat steps 4 in entirety with Marta's account on Marta's iPhone
       - **Critical privacy check:** Marta sees ONLY her own data (her plan, her weight, her workouts). Phase 2 family sync (FAM-*) ships in Plan 02-06 — for now, data isolation MUST hold via Phase 1 user-scoped queries.

    **STEFANO + MARTA TOGETHER — in person:**

    7. **§11: Tone calibration sign-off:**
       - Both iPhones in hand, both open `/today`
       - Compare side-by-side with `mockups/tone-calibration-v2/A-lifesum-pure.html` (Stefano can show on laptop)
       - Confirm fonts load correctly on both devices
       - Toggle dark mode (Settings → Tema → Scuro) — verify dark Lifesum Pure variant
       - Both confirm: NO "office app" drift, NO infantile mascots, NO `!` in error copy
       - Cross-reference `.planning/phases/01-foundation/01-08-tone-calibration-checklist.md` — variant A · Lifesum Pure officially LOCKED here (this closes the deferred Phase 1 T3 sign-off ritual)

    8. **§12: Sign-off table:**
       - Both reviewers fill: Initial, Date (YYYY-MM-DD), Time (HH:MM), Verdict (PASS / CONCERNS / BLOCK), Notes (any)
       - Closure rule: BOTH must be PASS for Phase 2 to progress to Plan 02-04. Any BLOCK → fix the underlying issue (re-run §1-§7 if deploy issue, file Plan 02-07 closure task if UI/UX issue).

    9. **Update `01-08-tone-calibration-checklist.md`** — append a "## Mid-Phase-2 Closure" section noting: "Variant A · Lifesum Pure locked at production deploy on {YYYY-MM-DD HH:MM} per Plan 02-03 §11 sign-off. Both reviewers PASS." Cross-link to 02-03-DEPLOY-CHECKLIST.md.

    10. **Commit signed checklist:**
        ```bash
        git add .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md \
                .planning/phases/01-foundation/01-08-tone-calibration-checklist.md
        git commit -m "docs(02-03): Stefano + Marta production deploy + iPhone install + tone-calibration sign-off — Variant A · Lifesum Pure LOCKED"
        ```

    **Verdict aggregation:**
    - PASS (both reviewers): Plan 02-03 closes. Plan 02-04 unblocked.
    - CONCERNS (either reviewer): Triage notes into Plan 02-07 closure todo list. Plan 02-04 may proceed if neither concern is deploy-blocking.
    - BLOCK (either reviewer): Stop. Fix root cause (deploy step or UI bug). Re-run from failed step. Re-checkpoint.
  </how-to-verify>
  <resume-signal>
    Type "approved — Stefano PASS, Marta PASS — checklist signed and committed" to resume Phase 2 execution. If verdict is CONCERNS, type the concern note + "approved with concerns". If BLOCK, type the blocker description and Plan 02-03 will be re-opened.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External internet → Windows Server 2019 :443 | Public HTTPS endpoint; reverse-proxy strips client info, FastAPI sees only IIS-forwarded headers |
| Stefano's iPhone Safari → wellness-buddy.epartner.it | First production user agent; PWA install flows through Apple SmartBanner / standalone display mode |
| Marta's iPhone Safari → wellness-buddy.epartner.it | Independent user; data isolation must hold via Phase 1 auth |
| `.env.production` filesystem | Contains JWT secret, DB password, CORS origins; readable only by NSSM service account + Stefano admin |
| Let's Encrypt → win-acme | TLS certificate provisioning; certificate stored in Windows Certificate Store (LocalMachine) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information disclosure | `.env.production` JWT/DB secrets | mitigate | `deploy/scripts/generate-secrets.ps1` writes file with NTFS ACL restricting to NSSM service account + Stefano admin (deploy script enforces). Sign-off table §3 verifies. |
| T-02-03-02 | Spoofing | `wellness-buddy.epartner.it` cert | mitigate | win-acme provisions Let's Encrypt cert + auto-renewal. §6 verifies "Connessione protetta" (no cert warnings, no mixed-content). External-network test (NOT LAN) verifies cert validity from outside. |
| T-02-03-03 | DoS | iPhone Safari + service worker conflicts on update | mitigate | Workbox skipWaiting:false (Phase 1 lock) — no auto-reload mid-form. Manual update flow shipped via FND-06 toast. |
| T-02-03-04 | Information disclosure | Marta sees Stefano's data | mitigate | Phase 1 user-scoped queries enforced (AUTH-04 access token tied to user_id; today_service / weight_service / workout_service all filter `user_id == user.id`). §9 sign-off explicitly verifies isolation. Phase 2 cross-user paths (Plan 02-06) NOT yet shipped — Marta CANNOT see Stefano's data via /api/weekly because Plan 02-02 doesn't expose `?user_id=` query yet. |
| T-02-03-05 | Tampering | Lighthouse score self-reported by Stefano | accept | Single-developer project; trust model assumes Stefano honest. §10 records actual scores in checklist (auditable; if scores fail, Plan 02-07 reviews). |
| T-02-03-06 | Repudiation | Sign-off table without timestamp | mitigate | §12 mandates Date (YYYY-MM-DD) + Time (HH:MM) per reviewer; commit log adds git-attested timestamp. |
| T-02-03-07 | Tampering | NSSM service running with elevated privileges | accept | Phase 1 deploy lock — service runs as `LocalSystem` to access `C:\msys64`. Phase 4 hardening reviews privilege model. |
| T-02-03-08 | DoS | Lighthouse PWA score <95 blocks closure | mitigate | If <95: investigate manifest icons / theme color / display:standalone / SW registration; fix and re-measure. Plan 02-07 captures any post-fix work. |
</threat_model>

<verification>
- `02-03-DEPLOY-CHECKLIST.md` committed with all 12 sections + sign-off table populated by both Stefano and Marta with PASS verdict
- `01-08-tone-calibration-checklist.md` updated with "Mid-Phase-2 Closure" section locking Variant A · Lifesum Pure
- `https://wellness-buddy.epartner.it/today` reachable from external network with valid TLS cert
- PWA installed on both Stefano's and Marta's iPhones (custom icon visible on home screen, standalone mode, no Safari chrome)
- Offline /today verified on at least one device (airplane mode → cached render → reconnect → ≤5s sync)
- Lighthouse PWA ≥95 + Accessibility ≥95 recorded in §10
- Marta verified she sees only her own data (data isolation holds via Phase 1 user-scoped queries)
- Refresh token rotation verified (kill app → reopen → still logged in)
- Tone calibration: both reviewers confirm Lifesum Pure rendering on real iPhones matches mockup expectation
- Plan 02-04, 02-05, 02-06, 02-07 unblocked
</verification>

<success_criteria>
1. `02-03-DEPLOY-CHECKLIST.md` exists with all 12 sections marked complete (every checkbox `[x]` for both Stefano + Marta passes)
2. Sign-off table §12: Stefano = PASS, Marta = PASS (both required for closure per D-26)
3. Lighthouse PWA score ≥95 recorded in §10 line 1; Accessibility ≥95 recorded in §10 line 2
4. `01-08-tone-calibration-checklist.md` reflects Variant A · Lifesum Pure LOCKED with mid-Phase-2 sign-off date + cross-reference to 02-03 checklist
5. PWA install verified on TWO real iPhones (Stefano's + Marta's), both showing standalone mode + offline /today
6. Marta data isolation verified (sees only her resources; Stefano's data NOT accessible)
7. `wellness-buddy.epartner.it` HTTPS responds 200 from external network
8. Commit on master: `docs(02-03): Stefano + Marta production deploy + iPhone install + tone-calibration sign-off — Variant A · Lifesum Pure LOCKED`
</success_criteria>

<output>
After completion, create `.planning/phases/02-differentiators/02-03-SUMMARY.md` documenting:
- Deploy date + git sha at deploy + Stefano's deploy elapsed time
- DEPLOY.md sections that needed clarification (Plan 02-07 captures fixes for next deploy)
- Lighthouse scores recorded (PWA / A11y / Performance / Best Practices) — exact numbers
- Any §11 tone calibration nits (filed for Plan 02-07 closure)
- 7-day GTK3 spike status update (this plan triggers Day 0 if not already opened by Plan 02-01)
- Phase 1 pause-gate items absorbed: real iPhone install ✓, Lighthouse PWA ≥95 ✓, tone sign-off ✓, upgrade path (FND-06) ✓
- Hand-off to Plan 02-04 (production environment confirmed working — shopping aggregation can ship)
- Hand-off to Plan 02-05 (production WeasyPrint verified working in §10 + spike Day N status — PDF endpoint can ship)
- Hand-off to Plan 02-06 (production data isolation verified — family sync activation can layer on)
- STATE.md decision-log entry: "(Plan 02-03) Production deploy verified by Stefano + Marta sign-off; Lifesum Pure locked; Phase 1 pause-gate dissolved; Plans 02-04/05/06/07 unblocked"
</output>
