---
phase: 02-differentiators
plan: 08
type: execute
wave: 8
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
  - "02-05"
  - "02-06"
  - "02-07"
files_modified:
  - .planning/phases/02-differentiators/VERIFICATION.md
  - .planning/phases/02-differentiators/02-08-TONE-REVIEW-CHECKLIST.md
  - .planning/phases/02-differentiators/02-01-GTK3-SPIKE.md
  - .planning/phases/02-differentiators/02-06-IPHONE-PDF-VERIFY.md
  - frontend/tests/e2e/family-convergence.spec.ts
  - frontend/tests/visual/light.spec.ts
  - frontend/tests/visual/dark.spec.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - UI-10
  - UI-11
  - UI-12
  - UI-13
  - UI-14
  - UI-15
  - UI-16
  - UI-17
  - UI-18
  - UI-19
  - UI-20
tags:
  - phase-2
  - closure
  - verification
  - tone-review
  - axe-core
  - lighthouse
  - integration
  - pause-gate
  - human-verify
  - checkpoint
must_haves:
  truths:
    - "VERIFICATION.md exists with Phase 2 success criteria 1-5 evaluated against committed code + tests + production deploy"
    - "axe-core CI green on /today + /settimana + /spesa (all states: populated, empty, dark mode)"
    - "Lighthouse PWA ≥95 + a11y ≥95 measured on production for /today, /settimana, /spesa"
    - "Phase 2 GTK3 spike Day 7 verdict recorded in 02-01-GTK3-SPIKE.md (PASS or FAIL with PDF_BACKEND flip)"
    - "PDF iPhone verify (Plan 02-06) Stefano sign-off completed in 02-06-IPHONE-PDF-VERIFY.md (4 surfaces × 7 accent strings)"
    - "Tone review checklist 02-08-TONE-REVIEW-CHECKLIST.md signed by Stefano + Marta confirming Phase 2 surfaces converge on Lifesum Pure variant A"
    - "Family-sync 8×5 authz matrix CI green (Plan 02-07 deliverable verified by re-run on master HEAD)"
    - "Convergence ≤5s e2e Playwright test green (FAM-09)"
    - "ROADMAP.md Phase 2 marked complete with link to VERIFICATION.md verdict"
    - "STATE.md updated: Phase 2 → completed; Phase 3 → ready to plan"
  artifacts:
    - path: ".planning/phases/02-differentiators/VERIFICATION.md"
      provides: "Goal-backward verifier for Phase 2 with per-success-criterion table + per-requirement table + pause-gate exit criteria"
      contains: "## 1. Test / Lint / Build Results"
      contains2: "## 2. Per Success Criterion Status"
      contains3: "Pause Gate"
    - path: ".planning/phases/02-differentiators/02-08-TONE-REVIEW-CHECKLIST.md"
      provides: "Stefano + Marta tone review sign-off for /week, /spesa, shopping PDF, condiviso badge, variant selector"
      contains: "Variant A · Lifesum Pure"
      contains2: "Stefano"
      contains3: "Marta"
    - path: "frontend/tests/e2e/family-convergence.spec.ts"
      provides: "Two-tab convergence test simulating Stefano + Marta share toggle race"
      contains: "FAM-09"
      contains2: "5s"
  key_links:
    - from: "VERIFICATION.md SC1-SC5"
      to: "ROADMAP.md Phase 2 success criteria 1-5"
      via: "Direct citation of ROADMAP lines 103-108"
      pattern: "SC[1-5]"
    - from: "02-08-TONE-REVIEW-CHECKLIST.md tone review"
      to: ".planning/phases/01-foundation/01-08-tone-calibration-checklist.md Variant A locked entry"
      via: "Cross-reference Phase 2 surfaces match Phase 1 locked variant"
      pattern: "Variant A.*Lifesum Pure"
---

<objective>
Phase 2 closure CHECKPOINT — Plans 02-01..02-07 implemented; this plan verifies the phase against ROADMAP §Phase 2 pause-gate exit criteria + ROADMAP §Phase 2 success criteria 1-5 (goal-backward), runs final integration tests + axe-core CI + Lighthouse production audits, captures Stefano + Marta tone review sign-off across new Phase 2 surfaces (/week, /spesa, shopping PDF, condiviso badge, variant selector), and updates ROADMAP + STATE to reflect Phase 2 complete + Phase 3 unlocked.

Purpose: Pause gate for Phase 2. Phase 3 (Engagement & Polish — mascot/Lottie/dashboard/push notifications) cannot start until this checkpoint resolves PASS. Captures any "concerns" notes from Plans 02-03 / 02-06 sign-offs into a closure todo list.

Output: Signed VERIFICATION.md with PASS verdict; signed tone review checklist; ROADMAP marked Phase 2 complete; STATE updated; Phase 3 transition unblocked. This plan has a `checkpoint:human-verify` blocking gate at end.
</objective>

<execution_context>
@C:/Users/bakko/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/bakko/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-differentiators/02-CONTEXT.md
@.planning/phases/02-differentiators/02-RESEARCH.md
@.planning/phases/02-differentiators/02-UI-SPEC.md
@.planning/phases/02-differentiators/02-01-GTK3-SPIKE.md
@.planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md
@.planning/phases/02-differentiators/02-06-IPHONE-PDF-VERIFY.md
@.planning/phases/01-foundation/VERIFICATION.md
@.planning/phases/01-foundation/01-08-tone-calibration-checklist.md
@frontend/tests/visual/light.spec.ts
@frontend/tests/visual/dark.spec.ts
@frontend/tests/e2e/today.spec.ts
@frontend/lighthouserc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 2 integration sweep (backend + frontend + axe + visual + Lighthouse) + draft VERIFICATION.md goal-backward</name>
  <files>.planning/phases/02-differentiators/VERIFICATION.md, frontend/tests/e2e/family-convergence.spec.ts</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 lines 82-114 — goal + 5 success criteria + pause gate verbatim)
    - .planning/REQUIREMENTS.md (WEEK-* SHOP-* FAM-* DEP-06 UI-* — must each appear at least once in plans 02-01..02-07; this plan re-verifies)
    - .planning/phases/01-foundation/VERIFICATION.md (Phase 1 verifier — analog format)
    - .planning/phases/02-differentiators/02-01-GTK3-SPIKE.md (Stefano fills Day 7 verdict — read current state)
    - .planning/phases/02-differentiators/02-03-DEPLOY-CHECKLIST.md (signed PASS state)
    - .planning/phases/02-differentiators/02-06-IPHONE-PDF-VERIFY.md (Stefano signs after deploy)
    - frontend/tests/e2e/today.spec.ts (analog e2e shape for new family-convergence.spec.ts)
  </read_first>
  <behavior>
    - Test 1: All Phase 2 backend tests pass — `cd backend && pytest tests/ -q` → ≥190 passed (134 prior + Phase 2 additions)
    - Test 2: All Phase 2 frontend tests pass — `cd frontend && pnpm test` → ≥90 passed
    - Test 3: axe-core CI passes for /today + /settimana + /spesa in light + dark on populated AND empty states
    - Test 4: Lighthouse PWA ≥95 + a11y ≥95 measured on production https://wellness-buddy.epartner.it for /today + /settimana + /spesa (3 routes × 2 metrics = 6 score lines)
    - Test 5: Visual regression all green (no diffs) on the now-final state of /today + /settimana + /spesa light + dark
    - Test 6: Two-tab convergence Playwright e2e: Tab A as Stefano patches dinner variant `B`, Tab B as Marta refetch on focus → MealCard for that meal updates ≤5s (FAM-09)
    - Test 7: VERIFICATION.md drafted with: §1 test/lint/build re-run results, §2 per-success-criterion (SC1-SC5), §3 per-requirement matrix (WEEK-* SHOP-* FAM-* DEP-06 UI-*), §4 pause gate exit criteria, §5 outstanding concerns (forwarded from 02-03 + 02-06 sign-offs)
  </behavior>
  <action>
    1. **Run backend test sweep**:
    ```bash
    cd backend && .venv/Scripts/python.exe -m pytest tests/ -q --tb=short
    ```
    Record passed count + any failures. Target: ≥190 (134 Phase 1 + 13 Plan 02-02 + 42 Plan 02-05 + 4 Plan 02-06 + 52 Plan 02-07 = ~245). Adjust expectation against actual.

    2. **Run frontend test sweep + lint + typecheck + visual + axe**:
    ```bash
    cd frontend && pnpm test && pnpm lint && pnpm typecheck && pnpm build && pnpm test:visual && pnpm test:axe
    ```
    All must be green. Record exit codes.

    3. **Lighthouse production audit** — Stefano runs Chrome DevTools Lighthouse on:
    - https://wellness-buddy.epartner.it/today
    - https://wellness-buddy.epartner.it/settimana/{thisWeek}
    - https://wellness-buddy.epartner.it/spesa/{thisWeek}

    Record (PWA / a11y / performance / best practices) per route. Target: PWA ≥95, a11y ≥95.

    4. **Create `frontend/tests/e2e/family-convergence.spec.ts`** — Playwright 2-context test simulating Stefano + Marta:
    ```typescript
    /**
     * FAM-09 convergence test — share toggle propagates ≤5s via TanStack Query refetchOnFocus + 30s staleTime.
     */
    import { test, expect } from '@playwright/test';

    test('Stefano variant change converges to Marta within 5s on focus', async ({ browser }) => {
      // Two browser contexts simulating two devices
      const stefanoCtx = await browser.newContext();
      const martaCtx = await browser.newContext();

      const stefanoPage = await stefanoCtx.newPage();
      const martaPage = await martaCtx.newPage();

      // Login both (test fixtures; tokens injected via storage state)
      await stefanoPage.goto('/login');
      await stefanoPage.fill('[data-test=email]', 'stefano@test.example.com');
      await stefanoPage.fill('[data-test=password]', 'Password123!');
      await stefanoPage.click('[data-test=submit]');
      await expect(stefanoPage).toHaveURL(/today/);

      await martaPage.goto('/login');
      await martaPage.fill('[data-test=email]', 'marta@test.example.com');
      await martaPage.fill('[data-test=password]', 'Password123!');
      await martaPage.click('[data-test=submit]');
      await expect(martaPage).toHaveURL(/today/);

      // Both navigate to /settimana (same week)
      const week = '2026-05-04';
      await stefanoPage.goto(`/settimana/${week}`);
      await martaPage.goto(`/settimana/${week}`);

      // Marta records initial dinner variant for Stefano's row (her cross-user view)
      // (Test fixture seeds Stefano + Marta in same group_brunelli; both have variant rows)
      const initial = await martaPage.locator('[data-test=meal-stefano-dinner-variant]').textContent();

      // Stefano taps variant pill → Opzione B
      await stefanoPage.click('[data-test=variant-stefano-dinner-pill]');
      await stefanoPage.click('[data-test=variant-option-B]');
      await expect(stefanoPage.locator('[data-test=variant-stefano-dinner-pill]')).toContainText('Opzione B');

      // Marta blurs and re-focuses to trigger refetchOnFocus
      await martaPage.evaluate(() => window.dispatchEvent(new Event('blur')));
      await martaPage.evaluate(() => window.dispatchEvent(new Event('focus')));

      // Wait at most 5s for Marta's UI to converge
      await expect(martaPage.locator('[data-test=meal-stefano-dinner-variant]')).toContainText('Opzione B', { timeout: 5_000 });

      await stefanoCtx.close();
      await martaCtx.close();
    });
    ```

    Adjust `data-test` selectors to match actual VariantSelector / MealCard markup. The test relies on test fixtures being available in the test database.

    5. **Run e2e**:
    ```bash
    cd frontend && pnpm test:e2e -- family-convergence.spec.ts
    ```

    6. **Draft `.planning/phases/02-differentiators/VERIFICATION.md`** following Phase 1 VERIFICATION.md shape (the file at `.planning/phases/01-foundation/VERIFICATION.md` is the analog). Structure:

    ```markdown
    ---
    phase: 02-differentiators
    verified: {timestamp}
    status: {human_needed | passed | failed}
    score: {X/5} success criteria + {Y/Z} requirements verified
    overrides_applied: 0
    re_verification: false
    human_verification:
      - test: "Stefano + Marta tone review sign-off (Plan 02-08 §11)"
        expected: "Both reviewers approve Phase 2 surfaces converge on Lifesum Pure variant A"
        why_human: "UI-20 + tone calibration mid-Phase-2 review of populated /week + /spesa + condiviso + PDF"
      - test: "Plan 02-01 GTK3 spike Day 7 verdict"
        expected: "Stefano signs PASS (5xx <2%) OR FAIL (PDF_BACKEND flip to reportlab)"
        why_human: "Production observability across 7-day window"
      - test: "Plan 02-06 iPhone Safari PDF accent verification"
        expected: "Stefano signs all 4 surfaces (Safari/Mail/Files/Adobe) for 7-string accent corpus"
        why_human: "Real device rendering check"
    ---

    # Phase 2: Differentiators — Verification Report

    **Phase Goal:** {ROADMAP §Phase 2 line 84 verbatim}

    **Verified:** {timestamp}
    **Status:** {status}
    **Repository HEAD:** {git rev-parse HEAD}
    **Re-verification:** No — initial verification

    ---

    ## 1. Test / Lint / Build Results (re-run on master HEAD)

    | Layer | Command | Result | Count |
    |-------|---------|--------|-------|
    | Backend pytest | `cd backend && pytest tests/ -q` | PASS / FAIL | {N} passed |
    | Frontend vitest | `cd frontend && pnpm test` | PASS / FAIL | {N} passed |
    | Frontend lint | `pnpm lint` | PASS / FAIL | 0 warnings |
    | Frontend typecheck | `pnpm typecheck` | PASS / FAIL | clean |
    | Frontend build | `pnpm build` | PASS / FAIL | dist generated |
    | Frontend visual | `pnpm test:visual` | PASS / FAIL | 0 diffs |
    | Frontend axe-core | `pnpm test:axe` | PASS / FAIL | 0 violations |
    | Frontend e2e family-convergence | `pnpm test:e2e family-convergence` | PASS / FAIL | 1 passed |
    | Lighthouse PWA /today | desktop Chrome on prod URL | PASS / FAIL | {score}/100 |
    | Lighthouse a11y /today | same | PASS / FAIL | {score}/100 |
    | Lighthouse PWA /settimana | desktop Chrome on prod URL | PASS / FAIL | {score}/100 |
    | Lighthouse a11y /settimana | same | PASS / FAIL | {score}/100 |
    | Lighthouse PWA /spesa | desktop Chrome on prod URL | PASS / FAIL | {score}/100 |
    | Lighthouse a11y /spesa | same | PASS / FAIL | {score}/100 |
    | OKLCH PDF drift gate | `python backend/scripts/check_pdf_template_oklch.py` | PASS / FAIL | 6/6 |
    | Authz matrix (FAM-08) | `pytest tests/integration/test_family_authz_matrix.py` | PASS / FAIL | 40 passed |
    | DST scheduler (Pitfall #17) | `pytest tests/integration/test_scheduler.py` | PASS / FAIL | 3 passed |
    | Alembic 0001 idempotence | `pytest tests/integration/test_alembic_0001.py` | PASS / FAIL | {N} passed |

    ---

    ## 2. Per Success Criterion Status (ROADMAP §Phase 2 lines 103-108)

    ### SC1 — Vista settimanale + variant selector funzionano (WEEK-*)

    **Status:** {PASS / PARTIAL / FAIL}

    **Evidence:**
    - `frontend/src/pages/Week.tsx` (Plan 02-02) renders /settimana with WeekPicker + WeeklyMacroRing + 7 day sections + VariantSelector pills
    - `backend/app/api/weekly.py` exports GET / + GET /summary + PATCH /variant
    - `backend/app/services/variant_service.py` upsert_variant with LWW + If-Unmodified-Since 409 detection
    - 13 backend tests (test_weekly_api.py + test_variant_service.py) verify CRUD + 409 + visibility default + summary
    - Vitest tests (WeekPicker, WeeklyMacroRing, VariantSelector, DayCompletionStrip, EmptyStateWeek) green

    ### SC2 — Lista spesa auto-generata + esportabile (SHOP-*)

    **Status:** {PASS / PARTIAL / FAIL}

    **Evidence:**
    - Plan 02-05: ingredient_parser + category_mapper + shopping_service.aggregate_for_week + APScheduler reset cron + /api/shopping endpoints + frontend /spesa route + BroadcastChannel multi-tab + 8 Phosphor icons
    - Plan 02-06: shopping_list.html template + WeasyPrint base64 inline fonts + /api/shopping/{w}/export-pdf endpoint live + iPhone verify artifact
    - 42 unit tests (ingredient_parser + category_mapper) + 6 shopping integration + 3 DST tests + 4 PDF tests = 55 backend tests
    - Plus 4 component vitest tests + composeTextExport unit test
    - 5xx rate during 7-day GTK3 spike: {actual %} (target <2%)

    ### SC3 — Multi-user family sync visibile (FAM-*)

    **Status:** {PASS / PARTIAL / FAIL}

    **Evidence:**
    - Plan 02-07: Alembic 0001_activate_groups + auth.register auto-creates household (Pitfall #16) + get_user_with_group_access dep + cross-user query param wiring on today/weekly/shopping + family.py PATCH share + 40-test authz matrix + frontend SharedBadge + ShareToggleMenu + MealCard extension + family.* copy + italianTimeAgo
    - 40-test authz matrix CI green: 8 endpoints × 5 scenarios; S3/S4/S5 all return 404 (V13)
    - Convergence e2e Playwright test green: ≤5s shared variant sync via refetchOnFocus + 30s staleTime

    ### SC4 — Conflict UX grazioso (FAM-04, FAM-05)

    **Status:** {PASS / PARTIAL / FAIL}

    **Evidence:**
    - Plan 02-02 ships LWW + If-Unmodified-Since header + 409 envelope `{detail, code: "version_conflict", conflicting_user}` + sonner ConflictToast with "Aggiornato da {nome}" + Ricarica action
    - frontend/src/lib/ifUnmodifiedSince.ts utility (extractConflictPartner, is409Conflict)
    - Pitfall #18 silent-loss UX validated: toast names partner; Ricarica re-fetches authoritative state
    - test_weekly_api.py::test_patch_variant_409_on_stale_if_unmodified_since green

    ### SC5 — WIN REQUISITE su nuovo terreno (UI-*)

    **Status:** {PASS / PARTIAL / FAIL}

    **Evidence:**
    - Tone review sign-off pending Stefano + Marta (Task 2 checkpoint of this plan)
    - axe-core CI green for /today + /settimana + /spesa light + dark
    - Lighthouse a11y ≥95 for all 3 routes
    - prefers-reduced-motion CI test extends Phase 1 list with /settimana + /spesa
    - Zero hex literals in frontend/src + backend/app/templates (grep gates green)
    - Zero direct `@phosphor-icons/react` imports outside facade (grep gate green)
    - Italian copy single-source: 95 new keys in copy.it.ts under week.* + shopping.* + family.* + sync.* + pwa.* namespaces; zero inline italian strings in components (grep gate)
    - Visual regression baselines current; light + dark green
    - Container queries on /week tablet+ desktop (UI-SPEC §6.3); /spesa flat single-column on all viewports per D-29

    ---

    ## 3. Per Requirement Status

    | Requirement | Plan | Status | Evidence |
    |-------------|------|--------|----------|
    | WEEK-01 | 02-02 | {PASS} | frontend/src/components/week/WeekPicker.tsx + Week.tsx route |
    | WEEK-02 | 02-02 | {PASS} | weekly_service.build_weekly_payload returns 7 days × 4 slots with macro target |
    | WEEK-03 | 02-02 | {PASS} | VariantSelector dropdown 3 options ("Opzione A" / "Opzione B" / "Pasta speciale") |
    | WEEK-04 | 02-02 | {PASS} | PATCH /api/weekly/{w}/variant + LWW + 409 |
    | WEEK-05 | 02-02 | {PASS} | GET /api/weekly/{w}/summary returns kcal_total + macros |
    | SHOP-01 | 02-05 | {PASS} | shopping_service.aggregate_for_week reads variants → ingredients → 5 categories |
    | SHOP-02 | 02-05 | {PASS} | aggregation merges (canonical_name, unit) keys; ingredient_parser + Pitfall #14 q.b. handling |
    | SHOP-03 | 02-05 | {PASS} | PATCH /api/shopping/{w}/check + Dexie cache_shopping + mutation_queue offline-first |
    | SHOP-04 | 02-05 | {PASS} | category_mapper 5 fixed categories + Frigo/Verdura/Dispensa/Condimenti/Integratori |
    | SHOP-05 | 02-05 | {PASS} | ShoppingViewToggle Per categoria / Per giorno + meal-context caption per row |
    | SHOP-06 | 02-05 | {PASS} | Copia testo button + composeTextExport + clipboard.writeText |
    | SHOP-07 | 02-06 | {PASS} | POST /api/shopping/{w}/export-pdf + WeasyPrint + iPhone Safari verify |
    | SHOP-08 | 02-05 | {PASS} | APScheduler weekly reset cron + DST tests + autoResetMonday toast |
    | FAM-01 | 02-07 | {PASS} | Alembic 0001 backfills households + auth.register auto-creates |
    | FAM-02 | 02-02, 02-07 | {PASS} | default_visibility_for: cene/pranzi → group_shared, colazione/spuntini → private |
    | FAM-03 | 02-07 | {PASS} | SharedBadge + tooltip + meal partner name |
    | FAM-04 | 02-02 | {PASS} | If-Unmodified-Since header + 409 LWW |
    | FAM-05 | 02-02 | {PASS} | sonner ConflictToast italian copy + Ricarica action |
    | FAM-06 | 02-07 | {PASS} | get_user_with_group_access dep on today + weekly + shopping + family |
    | FAM-07 | 02-07 | {PASS} | group_id NEVER in JWT — re-look-up DB; verified by ex_member scenario in authz matrix |
    | FAM-08 | 02-07 | {PASS} | 40-test negative authz matrix (8 endpoints × 5 scenarios) |
    | FAM-09 | 02-07 | {PASS} | TanStack Query refetchOnFocus + 30s staleTime; convergence ≤5s e2e green |
    | DEP-06 | 02-01 | {PASS} | GTK3 install in DEPLOY.md Appendix B + 7-day spike Day 7 verdict |
    | UI-01..UI-20 | All Phase 2 plans | {PASS / PARTIAL} | Tokens consumed everywhere, axe-core green, Lighthouse ≥95, visual baselines, italian formatting, Phosphor facade, no hex |

    ---

    ## 4. Pause Gate Exit Criteria (ROADMAP §Phase 2 line 110)

    | Criterion | Status | Evidence |
    |-----------|--------|----------|
    | Family-sync authz tests verdi (matrix completa) | {PASS} | test_family_authz_matrix.py 40/40 |
    | PDF Italian accents iPhone Safari + Mail.app | {PASS / FAIL} | 02-06-IPHONE-PDF-VERIFY.md Stefano sign-off |
    | Badge "condiviso" converge ≤5s | {PASS} | Playwright family-convergence.spec.ts |
    | WeasyPrint GTK3 stabile in produzione (5xx <2% in 7d) | {PASS / FAIL flip to reportlab} | 02-01-GTK3-SPIKE.md Day 7 verdict |

    ---

    ## 5. Outstanding Concerns (forwarded from earlier sign-offs)

    | Source | Concern | Severity | Disposition |
    |--------|---------|----------|-------------|
    | Plan 02-03 §10 | Lighthouse PWA score = {actual} (target ≥95) | {Resolved / Open} | {forwarded to Plan 02-XX or accepted} |
    | Plan 02-06 PDF accent verify | {Stefano notes} | {severity} | {disposition} |
    | Plan 02-01 GTK3 spike | {Day 7 trend} | {severity} | {disposition} |

    ---

    ## 6. Verdict

    **Phase 2 closure verdict:** {PASS / CONCERNS / BLOCK} (filled by Task 2 checkpoint after Stefano + Marta tone review)
    ```

    7. Commit: `docs(02-08-task-1): VERIFICATION.md draft + family-convergence e2e + integration sweep results (Phase 2 closure)`.
  </action>
  <verify>
    <automated>cd D:/Develop/AI/WellnessBuddy/backend && .venv/Scripts/python.exe -m pytest tests/ -q --tb=no 2>&1 | tail -5 && cd D:/Develop/AI/WellnessBuddy/frontend && pnpm test 2>&1 | tail -5 && pnpm test:visual 2>&1 | tail -3 && test -f .planning/phases/02-differentiators/VERIFICATION.md && grep -c "## [1-6]" .planning/phases/02-differentiators/VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/Scripts/python.exe -m pytest tests/ -q` returns 0 failures
    - `cd frontend && pnpm test` returns 0 failures
    - `cd frontend && pnpm lint && pnpm typecheck && pnpm test:visual && pnpm test:axe` all 0 failures
    - `test -f .planning/phases/02-differentiators/VERIFICATION.md` exits 0
    - `grep -c "## [1-6]\." .planning/phases/02-differentiators/VERIFICATION.md` returns ≥5 (sections 1-5 minimum)
    - `grep -c "SC[1-5]" .planning/phases/02-differentiators/VERIFICATION.md` returns ≥5
    - `grep -c "FAM-08\|WEEK-01\|SHOP-01\|DEP-06" .planning/phases/02-differentiators/VERIFICATION.md` returns ≥4 (per-requirement table populated)
    - `test -f frontend/tests/e2e/family-convergence.spec.ts` exits 0
  </acceptance_criteria>
  <done>
    Integration sweep run; VERIFICATION.md drafted with sections 1-5 populated against actual results; e2e family-convergence test green; commit on master. Verdict in section 6 awaits Task 2 tone review.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — Stefano + Marta tone review across Phase 2 surfaces + sign VERIFICATION.md verdict + update ROADMAP + STATE</name>
  <what-built>
    Plans 02-01..02-07 shipped /settimana + /spesa + family sync + PDF export + production deploy. Plans 02-03 (deploy) and 02-06 (PDF accent) had their own sign-off rituals. This checkpoint completes Phase 2 closure: Stefano + Marta review the FULL Phase 2 surface area on real iPhones, sign the tone review checklist, then sign VERIFICATION.md verdict. Phase 3 (engagement & polish) unblocks.
  </what-built>
  <how-to-verify>
    **STEFANO + MARTA TOGETHER — in person on real iPhones:**

    1. **Create `.planning/phases/02-differentiators/02-08-TONE-REVIEW-CHECKLIST.md`** with this content (Stefano scaffolds before review):

    ```markdown
    # Plan 02-08 — Phase 2 Tone Review (Stefano + Marta)

    **Review opened:** ____________
    **Production URL:** https://wellness-buddy.epartner.it
    **Locked design language:** Variant A · Lifesum Pure (`mockups/tone-calibration-v2/A-lifesum-pure.html`)
    **Cross-reference:** `.planning/phases/01-foundation/01-08-tone-calibration-checklist.md` (variant A locked Plan 02-03 §11)

    ## Surfaces under review

    Both reviewers open each surface on their installed PWA (iPhone Safari standalone) and judge against Lifesum Pure variant A.

    ### Surface 1 — `/today` populated (post Plan 02-07 SharedBadge)

    - [ ] Lifesum Pure rendering preserved (warm cream bg, Plus Jakarta + Geist Mono + Instrument Serif greeting, leaf-sage primary)
    - [ ] SharedBadge inline next to slot-label caption when partner shared meal — small Phosphor UsersThree + partner name + Radix Tooltip on tap
    - [ ] ShareToggleMenu (`⋯` DotsThreeOutline) present on owner's own meals — DropdownMenu opens with Switch
    - [ ] No "office app" tone drift; no `!` in error copy
    - [ ] Italian copy from copy.it.ts (no inline strings)

    ### Surface 2 — `/settimana` populated

    - [ ] WeekPicker chip row (5 chips, current ± 2 weeks) + jump-to-date popover with month grid
    - [ ] WeeklyMacroRing centered, 220px mobile / 260px desktop, 7-day completion strip below
    - [ ] 7 day sections (Lunedì → Domenica) with sticky day header
    - [ ] VariantSelector pill on each MealCard: 3 options (Opzione A / Opzione B / Pasta speciale)
    - [ ] Opzione A pill bg = leaf-50; Opzione B = surface-muted; Pasta speciale = coral-50
    - [ ] DropdownMenu shows MacroDisplay compact preview per option + active dot on current
    - [ ] Tap variant change → optimistic UI + sonner success "Variante aggiornata" (no `!`)
    - [ ] No infantile mascots or cliché (D-32 — AI widget invariato Phase 2)

    ### Surface 3 — `/spesa` populated (Per categoria default)

    - [ ] 5 category sections in fixed order: Frigo & Freschi (Snowflake) / Frutta & Verdura (Carrot) / Dispensa (Package) / Condimenti (Wine) / Integratori (Pill)
    - [ ] Each section header: Phosphor icon (kitchen-feel, NOT generic shapes) + name + count badge + CaretDown
    - [ ] ShoppingItemRow: shadcn Checkbox 24×24 visual / 44×44 hit area + name + quantity Geist Mono right-align
    - [ ] Checked state: leaf-50 row bg + strikethrough + opacity 0.5
    - [ ] ShoppingViewToggle (Per categoria / Per giorno) — segmented control switch works smoothly
    - [ ] Sticky bottom row: Copia testo (ghost) + Esporta PDF (primary leaf-500)
    - [ ] Reset settimana subwidget — confirm dialog uses leaf-500 confirm (NOT destructive red — D-7.3)

    ### Surface 4 — Shopping list PDF (download + open in iPhone Safari)

    - [ ] Tap Esporta PDF → spinner → download → preview opens
    - [ ] Header: "Lista spesa" Plus Jakarta 700 28pt + Instrument Serif italic 14pt date subtitle
    - [ ] Wordmark "Wellness Buddy" top-right Plus Jakarta 600 11pt with letter-spacing
    - [ ] Category headings leaf-700 Plus Jakarta 600 14pt + 2pt underline
    - [ ] Item rows: ☐ checkbox glyph (Geist Mono) + name (Plus Jakarta 500 12pt) + quantity (Geist Mono 11pt right-align)
    - [ ] Italian accents render correctly in body + headers + subtitle (cross-reference 02-06-IPHONE-PDF-VERIFY.md)
    - [ ] Feels like "lista plastificata per il frigo" — warm, NOT corporate Helvetica/Times

    ### Surface 5 — Conflict toast (synthetic test)

    - [ ] Open /settimana on Stefano + Marta same week, both tap variant on a SHARED dinner within 1s of each other
    - [ ] Second user gets sonner toast: heading "Aggiornato da {nome}" body "Ricarica per vedere l'ultima versione." action "Ricarica"
    - [ ] No `!` in toast copy
    - [ ] Tap Ricarica → query refetches, second user sees first user's variant choice

    ### Surface 6 — VariantSelector dropdown (`/settimana` open menu)

    - [ ] DropdownMenu opens 250ms ease-out-soft (motion budget UI-04)
    - [ ] 3 menu items at min 56px height (touch-safe)
    - [ ] Active variant has leaf-500 dot 6×6 + Phosphor Check 18px right
    - [ ] MacroDisplay compact per option: kcal · P · C · F Geist Mono 11px tabular

    ### Surface 7 — Condiviso badge (any /today or /settimana shared meal)

    - [ ] Inline pill with Phosphor UsersThree 14px + partner name 12px in surface-muted bg
    - [ ] Max 12 chars with ellipsis truncation
    - [ ] Tap → Radix Tooltip "Aggiornato da {nome} · {timeAgo}" 150ms fade
    - [ ] Tooltip auto-dismiss 4s OR ESC OR tap-outside

    ## Cross-cutting tone audit (UI-17, UI-19, UI-20)

    - [ ] No `!` in any error/info copy across Phase 2 surfaces
    - [ ] No infantile mascots/avocado tropes (deferred to Phase 3 mascot review per D-32)
    - [ ] Emoji ≤1-2 per screen (UI-19); zero emoji in chrome
    - [ ] Italian formatting: thousand-separator `.` (1.250 kcal), italian decimal `,` (1,5 kg), 24h time, NFC normalize
    - [ ] All copy from copy.it.ts (no inline italian)
    - [ ] Phosphor facade discipline: zero direct @phosphor-icons/react imports outside icons/index.ts
    - [ ] Zero hex literals: zero `#aabbcc` in frontend/src + backend/app/templates

    ## Sign-off

    | Reviewer | Initial | Date (YYYY-MM-DD) | Time (HH:MM) | Verdict (PASS/CONCERNS/BLOCK) | Notes |
    |----------|---------|-------------------|--------------|-------------------------------|-------|
    | Stefano  |         |                   |              |                               |       |
    | Marta    |         |                   |              |                               |       |

    **Closure rule (Phase 2 pause gate):** Both reviewers PASS → Phase 2 complete; Phase 3 unlocked.
    Any BLOCK → fix root cause; re-review.
    CONCERNS → file in Plan 03-XX backlog; Phase 2 still closes if not deploy-blocking.
    ```

    2. **Both Stefano + Marta open each of 7 surfaces above** on their iPhones; mark checkboxes per row; record any drift.

    3. **Surface 5 conflict test** — both reviewers same /settimana week, both tap a shared dinner variant within 1s; second user must see ConflictToast with "Aggiornato da {first user name}".

    4. **Sign §Sign-off** with initial + date + time + verdict (PASS / CONCERNS / BLOCK).

    5. **Update VERIFICATION.md §6 verdict** with Stefano + Marta combined verdict.

    6. **Update STATE.md**:
       - `phase`: 02-differentiators → completed
       - `progress.completed_phases`: 0 → 1 (or current count + 1)
       - `progress.total_plans`: include all 7 Phase 2 plans
       - Decision-log: add "(Plan 02-08) Phase 2 closure: Stefano + Marta tone review PASS; Phase 2 complete; Phase 3 unlocked."
       - Next Action: `/gsd:transition` → Phase 3 (Engagement & Polish — mascot/Lottie/dashboard/push notifications/DST)

    7. **Update ROADMAP.md** Phase 2 entry:
       - Append after `Plans: TBD`: `Plans: 8 plans (02-01..02-08) — see .planning/phases/02-differentiators/`
       - Append after `Pause Gate (exit criteria)`: `Status: PASSED YYYY-MM-DD — see VERIFICATION.md`
       - Mark `[x]` on each Phase 2 success criterion

    8. **Commit closure**:
    ```bash
    git add .planning/phases/02-differentiators/02-08-TONE-REVIEW-CHECKLIST.md \
            .planning/phases/02-differentiators/VERIFICATION.md \
            .planning/STATE.md \
            .planning/ROADMAP.md
    git commit -m "docs(02-08): Phase 2 closure — Stefano + Marta tone review PASS; VERIFICATION verdict; STATE + ROADMAP updated for Phase 3 transition"
    ```

    9. **`/gsd:transition`** — orchestrator picks up new STATE and prepares Phase 3 planning context.

    **Verdict aggregation:**
    - PASS (both reviewers): Phase 2 closes. STATE updated. Phase 3 planning unblocked. /gsd:transition runs.
    - CONCERNS (either reviewer): file concerns into Phase 3 backlog (Phase 3 owns mascot/Lottie/dashboard); Phase 2 still closes if not deploy-blocking; concerns logged in STATE.
    - BLOCK (either reviewer): Phase 2 stays open. Fix root cause via Plan 02-08 hot-fix or revise existing plan. Re-checkpoint.
  </how-to-verify>
  <resume-signal>
    Type "approved — Stefano PASS, Marta PASS — Phase 2 closed; ready for /gsd:transition" to mark Phase 2 complete.
    If CONCERNS, append the concern note(s) + "approved with concerns".
    If BLOCK, type the blocker description and Plan 02-08 will reopen.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 production deploy → Stefano + Marta review judgment | Single-developer trust model |
| VERIFICATION.md self-reported scores | Stefano honest about Lighthouse + 5xx rates |
| ROADMAP / STATE updates | Direct file edits; commit history is the audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Repudiation | Sign-off without timestamp | mitigate | Tone review + VERIFICATION verdict mandate Date + Time per reviewer; commit log adds git-attested timestamp. |
| T-02-08-02 | Tampering | Lighthouse score self-reported | accept | Single-developer project; Stefano honest. Numbers recorded in VERIFICATION §1 — auditable. |
| T-02-08-03 | Information disclosure | VERIFICATION.md committed publicly | accept | Verifier records test counts + scores + verdicts; no PII or secrets. |
| T-02-08-04 | DoS | e2e family-convergence flaky | mitigate | Test runs against test fixtures (deterministic seeds); 5s timeout; if flaky, mark @flaky in CI and create P3 stability ticket. |
| T-02-08-05 | Tampering | tone review checklist marks as PASS without actually checking | accept | Stefano + Marta honesty; physical co-presence + 2-reviewer requirement creates social verification. |
</threat_model>

<verification>
- All Phase 2 backend tests pass: `cd backend && pytest tests/ -q` → ≥190 passed
- All Phase 2 frontend tests pass: `cd frontend && pnpm test` → ≥90 passed
- Visual regression all green (no diffs)
- axe-core CI all green for /today + /settimana + /spesa light + dark
- Lighthouse production audit: PWA + a11y ≥95 for 3 routes
- e2e family-convergence test green (FAM-09 ≤5s)
- VERIFICATION.md drafted + signed by Stefano + Marta with PASS verdict
- 02-08-TONE-REVIEW-CHECKLIST.md signed PASS by both reviewers
- STATE.md updated: Phase 2 → completed
- ROADMAP.md updated: Phase 2 entry shows PASS + plan list
- Phase 3 transition unblocked
</verification>

<success_criteria>
1. `VERIFICATION.md` exists with §1 test/lint/build re-run + §2 SC1-SC5 + §3 per-requirement matrix + §4 pause-gate exit criteria + §5 outstanding concerns + §6 verdict
2. All 5 ROADMAP §Phase 2 success criteria evaluated PASS
3. All 43 phase requirements (WEEK-01..05, SHOP-01..08, FAM-01..09, DEP-06, UI-01..20) appear in §3 per-requirement table with PASS or PARTIAL status
4. Pause gate 4 exit criteria green: family-sync authz matrix, PDF iPhone accents, ≤5s convergence, GTK3 stability
5. `02-08-TONE-REVIEW-CHECKLIST.md` signed PASS by Stefano + Marta across 7 surfaces (1 today + 1 settimana + 2 spesa-views + 1 PDF + 1 conflict toast + 1 condiviso badge + 1 variant selector dropdown)
6. e2e family-convergence Playwright test green
7. STATE.md updated: phase 02-differentiators → completed; progress incremented; Phase 3 ready to plan
8. ROADMAP.md updated: Phase 2 marked complete with VERIFICATION.md link
9. Commit on master: `docs(02-08): Phase 2 closure — Stefano + Marta tone review PASS; VERIFICATION verdict; STATE + ROADMAP updated for Phase 3 transition`
</success_criteria>

<output>
After completion, create `.planning/phases/02-differentiators/02-08-SUMMARY.md` documenting:
- Phase 2 plan count + total LOC delta + commit count
- Test count: backend total + frontend total + e2e + axe + Lighthouse scores
- VERIFICATION.md verdict + per-success-criterion outcome
- Tone review verdict per surface (PASS / CONCERNS for each)
- GTK3 spike Day 7 final outcome (PDF_BACKEND value in production)
- Pitfalls addressed: #14 q.b. merge, #15 BroadcastChannel iOS Safari, #16 register auto-create group, #17 DST, #18 LWW silent loss, #19 visual baseline staleness
- Hand-off to Phase 3: dashboard KPI calculation reads adherence data from variants (Plan 02-02 schema); push reminder uses APScheduler pattern from Plan 02-05; mascot/Lottie review uses 02-08 tone review baseline
- Concerns deferred to Phase 3: any tone review notes Stefano+Marta flagged
- STATE.md final entry: "Phase 2 complete; 7/7 plans; tone calibration variant A locked Plan 02-03; Phase 3 (Engagement & Polish) ready to plan via /gsd:plan-phase"
</output>
