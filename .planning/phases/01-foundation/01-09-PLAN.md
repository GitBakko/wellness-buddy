---
phase: 01-foundation
plan: 09
type: execute
wave: 7
depends_on: [05a, 05b, 06, 07, 08]
files_modified:
  - frontend/package.json
  - frontend/src/styles/theme.css
  - frontend/src/styles/globals.css
  - frontend/src/components/today/MacroRing.tsx
  - frontend/src/components/today/MacroRing.test.tsx
  - frontend/src/components/today/MealCard.tsx
  - frontend/src/components/today/MacroDisplay.tsx
  - frontend/src/components/today/DayStatusIndicator.tsx
  - frontend/src/components/today/WeightQuickLog.tsx
  - frontend/src/components/today/WorkoutForm.tsx
  - frontend/src/components/ai/AIWidget.tsx
  - frontend/src/pages/Today.tsx
  - frontend/src/pages/History.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/services/today.ts
  - frontend/src/services/plans.ts
  - frontend/src/i18n/copy.it.ts
  - frontend/src/components/icons/index.ts
  - frontend/src/index.html
  - backend/app/schemas/today.py
  - backend/app/services/today_service.py
  - backend/app/parsers/plan_parser.py
  - backend/app/schemas/plan_parsed.py
  - .planning/phases/01-foundation/01-08-tone-calibration-checklist.md
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-08, UI-20, TODAY-01, TODAY-02, TODAY-03, TODAY-04, FND-09]
must_haves:
  truths:
    - "theme.css OKLCH palette migrated to Lifesum Pure: warmer cream bg, sage-leaf 150-hue, refined coral 30-hue, new blueberry-purple 280-hue, new amber 75-hue families"
    - "Plus Jakarta Sans loaded as primary --font-sans (Geist Mono kept for tabular nums; Instrument Serif kept for display escape hatch)"
    - "@phosphor-icons/react installed, custom icon facade in components/icons/index.ts (no direct phosphor imports anywhere else — keeps swap path clean)"
    - "MacroRing.tsx component renders 4-arc SVG (kcal outer + P/C/F inner) consuming token colors, accepts {kcalConsumed, kcalTarget, protein, carbs, fat} props with target/current pairs"
    - "/today page integrates MacroRing as hero between greeting and meals list"
    - "MealCard supports optional photo_url prop, renders 80x80 rounded image when present, falls back to gradient placeholder when null — NO Storyset, NO emoji, only Phosphor icons for slot indicator"
    - "Backend MealItem schema includes photo_url: str | None (nullable) — parser leaves null for Phase 1, populated in Phase 2 plan editor or upload flow"
    - "All components consume new tokens — zero hardcoded hex anywhere in src/"
    - "Italian copy still source-of-truth in copy.it.ts, no inline strings introduced"
    - "Dark mode parity maintained — every new color has dark variant"
    - "tone-calibration-checklist.md locked with Variante A (Lifesum Pure) marked APPROVED"
    - "Plan 08 Task 3 production deploy + iPhone install + Lighthouse PWA: explicitly DEFERRED to Phase 2 pause gate per user decision; checklist documents the deferral"
  artifacts:
    - path: "frontend/src/components/today/MacroRing.tsx"
      provides: "Lifesum-signature circular macro tracker with 4 concentric arcs"
      contains: "stroke-dasharray"
    - path: "frontend/src/styles/theme.css"
      provides: "Lifesum Pure OKLCH token system"
      contains: "--color-blueberry-500"
    - path: "frontend/src/components/icons/index.ts"
      provides: "Phosphor icon facade — single import surface"
      contains: "@phosphor-icons/react"
  key_links:
    - from: "frontend/src/pages/Today.tsx"
      to: "frontend/src/components/today/MacroRing.tsx"
      via: "Direct import, hero element above meal list"
      pattern: "MacroRing"
    - from: "frontend/src/components/today/MealCard.tsx"
      to: "frontend/src/components/icons/index.ts"
      via: "Phosphor icon facade for slot indicator + check"
      pattern: "from '@/components/icons'"
---

<objective>
Propagate the **Lifesum Pure** design language (locked variant A) from `mockups/tone-calibration-v2/A-lifesum-pure.html` into production source. This is the Phase 1 closure plan that fulfills the WIN REQUISITE UI/UX commitment in code, not just in mockups.

The mockup-to-production gap is real: production currently runs Geist Sans + minimal coral/leaf palette + no macro ring + no Phosphor icons + no food photography. After this plan: production /today renders the macro ring, Plus Jakarta Sans body, food photography in MealCard, Phosphor iconography, refined Lifesum-faithful palette — token-identical to the approved mockup.

Build order: tokens before components. Phase 2 must inherit the locked theme so meal variants + shopping list + family sync ship on the right foundation.

Output: opening production /today (logged-in Stefano with active plan) renders visually identical to `A-lifesum-pure.html` mobile-light-populated frame.
</objective>

<execution_context>
@C:/Users/bakko/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/bakko/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@d:/Develop/AI/WellnessBuddy/.planning/phases/01-foundation/01-CONTEXT.md
@d:/Develop/AI/WellnessBuddy/.planning/phases/01-foundation/01-UI-SPEC.md
@d:/Develop/AI/WellnessBuddy/.planning/phases/01-foundation/01-VALIDATION.md
@d:/Develop/AI/WellnessBuddy/mockups/tone-calibration-v2/A-lifesum-pure.html
@d:/Develop/AI/WellnessBuddy/.planning/phases/01-foundation/01-07-PLAN.md
</context>

<interfaces>
From Plan 02:
- `app/models/{user,plan,workout,weight}.py`
- `app/core/deps::{get_current_user, get_session}`

From Plan 04:
- `app/schemas/plan_parsed.py::PlanParsedSchema` — extend MealItem with photo_url

From Plan 07:
- `app/schemas/today.py` — extend TodayPayload meal items with photo_url
- `app/services/today_service.py::build_today_payload`
- `frontend/src/services/today.ts::useTodayQuery, useCompleteMeal`
- `frontend/src/services/weight.ts`, `frontend/src/services/workout.ts`
- `frontend/src/components/today/{MealCard,MacroDisplay,DayStatusIndicator,WeightQuickLog,WorkoutForm}.tsx`
- `frontend/src/pages/{Today,History,Settings}.tsx`

From mockup A reference:
- `mockups/tone-calibration-v2/A-lifesum-pure.html` — production target. Source-of-truth for: macro ring SVG structure, Plus Jakarta typography hierarchy, palette OKLCH coords, Phosphor icon choices, food-photo card layout, light/dark parity, empty state.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tokens + fonts + dependencies — theme.css migration to Lifesum Pure palette</name>
  <files>frontend/package.json, frontend/src/styles/theme.css, frontend/src/styles/globals.css, frontend/src/index.html</files>
  <read_first>
    - mockups/tone-calibration-v2/A-lifesum-pure.html (the `:root` block under `.variant-lifesum-pure` defines the locked tokens)
    - frontend/src/styles/theme.css (current state — preserve `@theme` block structure, replace OKLCH coords)
    - frontend/package.json (existing deps to know what NOT to overwrite)
  </read_first>
  <behavior>
    No tests for this task — pure token swap + dep install. Verification by lint + typecheck + build pass.
  </behavior>
  <action>
    1. **Install deps** in `frontend/`:
       ```
       pnpm add @phosphor-icons/react @fontsource-variable/plus-jakarta-sans
       ```
       Keep existing `@fontsource-variable/geist-mono` for tabular numerics (macro values, weight log, time).
       Keep existing `@fontsource/instrument-serif` for /today greeting display escape hatch (UI-SPEC §3 still permits it).
       Remove `@fontsource-variable/geist` if installed — Plus Jakarta replaces Geist Sans entirely as primary `--font-sans`.

    2. **Update `frontend/src/styles/theme.css`** — preserve all existing variable NAMES, replace OKLCH coords + add new color families:

       Light mode `:root` `@theme` block:
       ```css
       /* Backgrounds — warmer cream */
       --color-bg: oklch(97% 0.012 85);                /* was 98.5% 0.005 80 */
       --color-bg-elev: oklch(99% 0.005 85);           /* was 100% 0 0 */
       --color-surface: oklch(99% 0.005 85);
       --color-surface-muted: oklch(94% 0.015 85);

       /* Neutrals — slightly warmer hue, slate-ink for text */
       --color-neutral-50: oklch(97% 0.012 85);
       --color-neutral-100: oklch(94% 0.015 85);
       --color-neutral-200: oklch(90% 0.012 85);
       --color-neutral-300: oklch(82% 0.012 85);
       --color-neutral-400: oklch(70% 0.012 240);      /* slate hue start */
       --color-neutral-500: oklch(58% 0.014 240);
       --color-neutral-600: oklch(50% 0.015 240);
       --color-neutral-700: oklch(38% 0.015 240);
       --color-neutral-800: oklch(28% 0.015 240);
       --color-neutral-900: oklch(22% 0.015 240);
       --color-neutral-950: oklch(16% 0.012 240);

       /* Coral — refined hue 30 (was 28) */
       --color-coral-50: oklch(96% 0.025 30);
       --color-coral-100: oklch(94% 0.05 30);
       --color-coral-200: oklch(86% 0.09 30);
       --color-coral-300: oklch(80% 0.115 30);
       --color-coral-400: oklch(75% 0.135 30);
       --color-coral-500: oklch(72% 0.14 30);          /* DEFAULT — slightly more luminous than before */
       --color-coral-600: oklch(62% 0.16 28);
       --color-coral-700: oklch(52% 0.16 28);
       --color-coral-800: oklch(40% 0.135 28);
       --color-coral-900: oklch(30% 0.10 28);

       /* Leaf-Sage — refined hue 150 (was 145), the Lifesum signature green */
       --color-leaf-50: oklch(94% 0.04 150);
       --color-leaf-100: oklch(92% 0.05 150);
       --color-leaf-200: oklch(85% 0.08 150);
       --color-leaf-300: oklch(78% 0.10 150);
       --color-leaf-400: oklch(70% 0.12 150);
       --color-leaf-500: oklch(65% 0.13 150);          /* DEFAULT */
       --color-leaf-600: oklch(58% 0.13 150);
       --color-leaf-700: oklch(48% 0.13 150);
       --color-leaf-800: oklch(38% 0.11 150);
       --color-leaf-900: oklch(28% 0.085 150);

       /* NEW — Blueberry purple, used for protein arc on macro ring + protein chip */
       --color-blueberry-50: oklch(96% 0.025 280);
       --color-blueberry-200: oklch(86% 0.07 280);
       --color-blueberry-500: oklch(58% 0.13 280);     /* DEFAULT */
       --color-blueberry-700: oklch(42% 0.13 280);

       /* NEW — Amber, used for fat arc on macro ring + fat chip */
       --color-amber-50: oklch(96% 0.04 75);
       --color-amber-200: oklch(88% 0.09 75);
       --color-amber-500: oklch(75% 0.13 75);          /* DEFAULT */
       --color-amber-700: oklch(58% 0.14 70);

       /* Semantic — re-anchored to new leaf hue */
       --color-success: oklch(60% 0.13 150);
       --color-success-bg: oklch(94% 0.05 150);
       --color-warning: oklch(75% 0.13 75);
       --color-warning-bg: oklch(96% 0.04 75);
       --color-destructive: oklch(58% 0.18 25);
       --color-destructive-bg: oklch(95% 0.04 25);

       /* Text — slate ink on warm cream */
       --color-text: oklch(22% 0.015 240);             /* was 28% 0.01 80 */
       --color-text-muted: oklch(50% 0.015 240);
       --color-text-inverse: oklch(97% 0.012 85);

       --color-border: oklch(89% 0.012 85);
       --color-focus-ring: oklch(48% 0.10 150);        /* moved to leaf hue, more on-brand */
       ```

       Fonts:
       ```css
       --font-sans: 'Plus Jakarta Sans Variable', 'Plus Jakarta Sans', system-ui, sans-serif;
       --font-mono: 'Geist Mono Variable', 'Geist Mono', ui-monospace, monospace;
       --font-display: 'Instrument Serif', 'Plus Jakarta Sans Variable', serif;
       ```

       shadcn alias block — re-anchor to new tokens (same shape as before, just new oklch coords matching above).

       Dark-mode `@media` block + `:root[data-theme='dark']` block — mirror the new light tokens with dark variants:
       ```css
       --color-bg: oklch(18% 0.015 250);
       --color-bg-elev: oklch(22% 0.018 250);
       --color-surface: oklch(22% 0.018 250);
       --color-surface-muted: oklch(26% 0.02 250);
       --color-neutral-200: oklch(30% 0.015 250);
       --color-neutral-700: oklch(75% 0.012 240);
       --color-neutral-800: oklch(88% 0.008 80);
       --color-coral-500: oklch(76% 0.14 30);
       --color-coral-600: oklch(82% 0.13 30);
       --color-leaf-500: oklch(72% 0.13 150);
       --color-leaf-700: oklch(60% 0.13 150);
       --color-blueberry-500: oklch(72% 0.12 280);
       --color-amber-500: oklch(82% 0.12 75);
       --color-text: oklch(94% 0.005 80);
       --color-text-muted: oklch(75% 0.012 240);
       --color-border: oklch(30% 0.015 250);
       /* shadcn aliases mirrored same as before */
       ```

    3. **Update `frontend/src/styles/globals.css`** — import Plus Jakarta:
       ```css
       @import '@fontsource-variable/plus-jakarta-sans';
       @import '@fontsource-variable/geist-mono';
       @import '@fontsource/instrument-serif';
       ```
       Remove the Geist Sans import.

    4. **Update `frontend/index.html`** — preconnect to Google Fonts removed; rely entirely on @fontsource bundled fonts. Update meta theme-color to match new bg (`oklch(97% 0.012 85)` → compute hex equivalent and put in meta — actually use CSS `light-dark()` or set meta dynamically; for now set to a static near-cream `#F8F4ED` — NO wait, hex banned in src. Put meta theme-color via JS in App.tsx that reads getComputedStyle of body bg. Actually simplest: leave meta as-is for now, document follow-up).

    5. **No code changes outside these 4 files in this task.** Components keep using `var(--color-*)` so they automatically pick up new oklch values.
  </action>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0 (no new hex hits)
    - `pnpm build` exits 0
    - `grep -E "#[0-9a-fA-F]{3,8}\b" frontend/src/styles/theme.css frontend/src/styles/globals.css` → 0 matches
    - `pnpm test` still 48/48 (existing tests don't reference token values)
    - `frontend/package.json` contains `@phosphor-icons/react` and `@fontsource-variable/plus-jakarta-sans`
    - `theme.css` contains `--color-blueberry-500` and `--color-amber-500`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MacroRing component + icon facade + integration into /today</name>
  <files>frontend/src/components/today/MacroRing.tsx, frontend/src/components/today/MacroRing.test.tsx, frontend/src/components/icons/index.ts, frontend/src/pages/Today.tsx, frontend/src/components/today/MealCard.tsx, frontend/src/components/today/MacroDisplay.tsx, frontend/src/components/today/DayStatusIndicator.tsx, backend/app/schemas/today.py, backend/app/schemas/plan_parsed.py, backend/app/parsers/plan_parser.py, backend/app/services/today_service.py</files>
  <read_first>
    - mockups/tone-calibration-v2/A-lifesum-pure.html (lines containing `class="macro-ring"` SVG — exact target structure; lines containing `meal-card` for photo+info+check layout)
    - frontend/src/components/today/MealCard.tsx (current implementation)
    - backend/app/schemas/today.py (current TodayPayload + MealItem)
    - backend/app/schemas/plan_parsed.py (current MealItem in parsed plan)
  </read_first>
  <behavior>
    Tests required (Vitest):
    - **MacroRing renders 4 concentric SVG circles + 4 progress arcs**: smoke test — mount with kcalConsumed=1760, kcalTarget=2200, protein={current:78,target:120}, carbs={current:165,target:250}, fat={current:52,target:75} → expect 8 circles in SVG (4 tracks + 4 progress arcs)
    - **MacroRing renders kcal value with tabular nums**: expect "1.760" in DOM with `font-variant-numeric: tabular-nums`
    - **MacroRing handles 0 progress safely**: kcalConsumed=0 → progress arc has stroke-dasharray "0 ..." (no render error)
    - **MacroRing handles overflow safely**: kcalConsumed > kcalTarget → caps at 100% (no negative dash offset)
    - **MacroRing accessible label**: container has `role="img"` + `aria-label` matching pattern "1.760 di 2.200 kcal oggi"
    - **MealCard renders photo_url when provided**: photo URL in src
    - **MealCard renders gradient placeholder when photo_url null**: no img, has aria-hidden gradient div
    - **MealCard slot icon uses Phosphor**: smoke test that the icon import resolves, renders an SVG (not an img tag with emoji)
  </behavior>
  <action>
    1. **`frontend/src/components/icons/index.ts`** — Phosphor facade (single import surface, keeps swap path clean):
       ```typescript
       // Re-export only the icons used in /today + History + Settings.
       // Adding a new icon? Add it here, never import @phosphor-icons/react elsewhere.
       export {
         Leaf,
         BowlFood,
         Fish,
         Cookie,
         OrangeSlice,
         PersonSimpleRun,
         Scales,
         House,
         CalendarBlank,
         ShoppingCart,
         ClockCounterClockwise,
         User,
         CheckCircle,
         Circle,
         Sparkle,
         ArrowDown,
         ArrowUp,
         UploadSimple,
         Plus,
         Sun,
         Moon,
         X,
         CaretDown,
         CaretUp,
         CaretLeft,
         CaretRight,
       } from '@phosphor-icons/react';
       ```

    2. **`frontend/src/components/today/MacroRing.tsx`** — write fresh, mirror the SVG structure from mockup A:
       ```tsx
       interface MacroValue { current: number; target: number }
       interface Props {
         kcalConsumed: number;
         kcalTarget: number;
         protein: MacroValue;
         carbs: MacroValue;
         fat: MacroValue;
         size?: number; // default 220
       }
       export function MacroRing({ ... }: Props) {
         // Ratios clamped to [0, 1]
         // SVG: 4 track circles + 4 progress arcs at radii 86 / 68 / 54 / 40
         // Track strokes: var(--color-leaf-100) / blueberry-50 / leaf-50 / amber-50
         // Progress strokes: --color-leaf-500 / --color-blueberry-500 / --color-leaf-700 / --color-amber-500
         // stroke-linecap="round", transform="rotate(-90 100 100)"
         // Center: stacked text block, kcal big (font-display escape hatch is FORBIDDEN here — keep Plus Jakarta 800), kcalTarget caption
         // role="img" + aria-label using copy.today.macroRingAria with replacements
       }
       ```
       Add Italian copy keys to copy.it.ts:
       ```ts
       today: {
         ...,
         macroRingAria: '{consumed} di {target} kcal oggi',
         macroKcalSuffix: 'kcal oggi',
         macroKcalSubtitle: 'su {target}',
       }
       ```

    3. **Backend schema extension** — add `photo_url: str | None = None` field:
       - `backend/app/schemas/plan_parsed.py::MealItem` — new field, default None, validates as URL string when set
       - `backend/app/schemas/today.py::TodayMeal` — same `photo_url: str | None = None`
       - `backend/app/services/today_service.py::build_today_payload` — pass through `meal.photo_url` from parsed plan to TodayMeal
       - `backend/app/parsers/plan_parser.py` — parser extracts optional `**Foto:** <url>` line from each meal section if present (regex `^\*\*Foto:\*\*\s+(\S+)$`); sets photo_url to that URL or None. Update unit tests in `backend/tests/unit/test_parser_evil.py` if needed (parser must still pass on existing 6 evil-corpus fixtures — none have photos).

    4. **`frontend/src/services/today.ts`** — TodayMeal TS interface gets `photo_url: string | null` field.

    5. **`frontend/src/components/today/MealCard.tsx`** — restructure to Lifesum-Pure layout:
       - 80×80 photo on left (radius `var(--radius-md)`); when `photo_url` null, render gradient placeholder div with `background: linear-gradient(135deg, var(--color-leaf-100), var(--color-leaf-50))` + small Phosphor icon centered (BowlFood / Fish / Cookie / OrangeSlice based on meal_type)
       - Middle column: meal slot caption (uppercase Italian via `copy.today.mealLabels`) + meal name (semibold) + macro chips inline
       - Right side: 44×44 circular check button — outline `var(--color-border)` when pending, filled `var(--color-leaf-500)` with white CheckCircle Phosphor icon when completed; clicking calls `onToggle` (preserve current API)
       - Use Phosphor icons via `@/components/icons`, NEVER inline SVG checkmark, NEVER emoji
       - Tap target ≥44px on the check button (UI-06 preserved)
       - All copy from `copy.today.*`

    6. **`frontend/src/components/today/MacroDisplay.tsx`** — re-style chips:
       - Three pills horizontally: Proteine (blueberry-500 bg @ 8% opacity), Carboidrati (leaf-500 bg @ 8% opacity), Grassi (amber-500 bg @ 8% opacity)
       - Each shows current/target value + tiny linear progress bar
       - Italian labels from `copy.today.macroProtFull`, `macroCarbFull`, `macroFat`
       - Tabular nums via Geist Mono

    7. **`frontend/src/components/today/DayStatusIndicator.tsx`** — preserve API, restyle: completed=leaf-500 pill, partial=neutral-200 pill, planned=neutral-100 pill outline. NEVER red. Per UI-SPEC.

    8. **`frontend/src/pages/Today.tsx`** — restructure /today layout to match mockup A populated state:
       - Top: PageHeader-style row with greeting (Plus Jakarta 700, time-aware Italian) + score pill (Phosphor Leaf icon + "Buon ritmo")
       - Hero: `<MacroRing>` centered, fed from useTodayQuery aggregator data
       - Section heading "I tuoi pasti"
       - Meal list (uses MealCard)
       - Two-column grid below (or stacked on mobile): WeightQuickLog + WorkoutForm
       - AI widget at bottom (unchanged Plan 06 locked placeholder, just consume new tokens)
       - Bottom tab bar: Phosphor icons via icon facade
  </action>
  <acceptance_criteria>
    - All new Vitest tests pass (8 new MacroRing + MealCard tests minimum)
    - Existing 48 frontend tests still pass
    - Backend tests still 134/134 pass (parser extension non-breaking, default None)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0 (zero hex hits)
    - Dev server boots, /today renders without console errors
    - Visual sanity: open `mockups/tone-calibration-v2/A-lifesum-pure.html` and prod /today side-by-side at 390px — they match in macro ring shape + palette + typography hierarchy
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remaining components + tone calibration lock + Plan 08 T3 deferral note</name>
  <files>frontend/src/components/today/WeightQuickLog.tsx, frontend/src/components/today/WorkoutForm.tsx, frontend/src/components/ai/AIWidget.tsx, frontend/src/components/weight/WeightChart.tsx, frontend/src/components/weight/WeightHistoryTable.tsx, frontend/src/components/workout/WorkoutHistoryTable.tsx, frontend/src/pages/History.tsx, frontend/src/pages/Settings.tsx, frontend/src/components/layout/AppShell.tsx, frontend/src/components/layout/BottomTabBar.tsx, frontend/src/components/layout/AppBar.tsx, .planning/phases/01-foundation/01-08-tone-calibration-checklist.md</files>
  <read_first>
    - mockups/tone-calibration-v2/A-lifesum-pure.html (full file — every component reference)
    - All current component files listed in <files>
    - .planning/phases/01-foundation/01-08-tone-calibration-checklist.md (checklist to lock)
  </read_first>
  <action>
    1. **`WeightQuickLog.tsx`** — restyle: `var(--radius-card)` panel, Phosphor Scales icon header, italian decimal input with Plus Jakarta + Geist Mono for the placeholder value, leaf-500 submit button. Italian decimal parsing (parseItalianDecimal "75,3") preserved.

    2. **`WorkoutForm.tsx`** — restyle: Phosphor PersonSimpleRun icon header, switch tap-friendly (≥44px), conditional fields shown when trained=true, leaf-500 submit. Preserve all field semantics.

    3. **`AIWidget.tsx`** — preserve locked-placeholder behavior (no network, no data). Restyle: Phosphor Sparkle icon glyph chip with leaf-50 bg, italic-serif "Suggerimenti AI" heading (font-display escape hatch — actually keep sans Plus Jakarta 600 since UI-SPEC §3 caps display-serif at 1/page and Today greeting already uses it). Caption "presto disponibile" muted. Soft dashed border `var(--color-border)`.

    4. **`WeightChart.tsx`** — Recharts stroke `var(--color-neutral-700)` already, no change needed except verify still works on new neutral-700 oklch coords. Verify visual appearance.

    5. **`WeightHistoryTable.tsx`**, **`WorkoutHistoryTable.tsx`** — restyle row hover bg to `var(--color-surface-muted)`, edit/delete action buttons use Phosphor X / Plus icons.

    6. **`History.tsx`** — restyle: PageHeader Plus Jakarta 700, Tabs use Lifesum-style soft pills with leaf-500 active state.

    7. **`Settings.tsx`** — restyle: theme picker uses Phosphor Sun/Moon icons, locked Italian language note shown in muted card, logout button leaf-500 outlined.

    8. **`AppShell.tsx`**, **`AppBar.tsx`**, **`BottomTabBar.tsx`** — restyle bar borders/bg via tokens; bottom tabs use Phosphor icons via facade; active tab indicator leaf-500.

    9. **`.planning/phases/01-foundation/01-08-tone-calibration-checklist.md`** — append a "DECISION" section at the bottom:
       ```markdown
       ## DECISION (User Sign-Off)

       **Date:** 2026-05-02
       **Decided by:** Stefano Brunelli (project owner)
       **Locked variant:** **A · Lifesum Pure** (mockups/tone-calibration-v2/A-lifesum-pure.html)
       **Rationale:**
       - Lifesum is the established visual reference; users (Stefano + Marta) already understand the language
       - Macro ring as hero element validated as core focal point
       - Sage + cream + coral palette: warm-clinical, premium without being aggressive
       - Plus Jakarta Sans + Phosphor Icons set the friendly-rounded foundation across the app

       ### Tasks deferred to Phase 2 pause gate
       Production-bound verifications (Plan 08 Task 3 sub-items) are explicitly **DEFERRED** to the Phase 2 pause gate per project owner decision (2026-05-02):
       - Real iPhone install + offline /today verification
       - Lighthouse PWA 100/100 score
       - DEPLOY.md walk-through on Windows Server 2019
       - win-acme cert for `wellness-buddy.epartner.it`
       - Stefano + Marta in-person sign-off ritual

       Reasoning: variant A is locked in code (Plan 01-09 propagation), so Phase 2 (variants + shopping list + family sync) ships on the right design foundation. Production deploy + iPhone install + Lighthouse run combined with Phase 2 deliverables at the next pause gate.

       Plan 01-09 fulfills the **code-side** half of the Phase 1 pause gate ("tone calibration locked" → variant A propagated to source). Plan 08 Task 3 production-side criteria reclassified as **Phase 2 pause gate prerequisites**.
       ```
  </action>
  <acceptance_criteria>
    - All 48+ existing frontend tests still pass
    - Backend 134 tests still pass
    - `pnpm typecheck` + `pnpm lint` + `pnpm build` all exit 0
    - `grep -E "#[0-9a-fA-F]{3,8}\b" frontend/src` → 0 hits
    - `grep -E "@phosphor-icons/react" frontend/src --include='*.tsx' --include='*.ts' | grep -v components/icons/index.ts` → 0 hits (facade respected)
    - Dev server runs, full app navigates: login → /today (with macro ring) → /storico → /impostazioni without console errors
    - Tone calibration checklist contains "Locked variant: A · Lifesum Pure" and "DEFERRED to Phase 2 pause gate"
  </acceptance_criteria>
</task>

</tasks>

## Trust Boundaries

This plan is purely cosmetic + structural — no new privileged code paths, no new external calls, no auth changes. Backend MealItem.photo_url is data-only with URL string validation in Pydantic v2 (`HttpUrl` if external, `str` if internal — keep `str | None` lenient because Phase 2 owns the upload flow).

## STRIDE Threat Register (Plan 01-09 scope)

| Threat | Vector | Mitigation | Verified by |
|---|---|---|---|
| Spoofing | photo_url could be attacker-controlled URL leading to malicious image | Pydantic field validates as string (length cap 500); rendering uses `<img src>` only — no eval/dangerouslySetInnerHTML; CSP `img-src 'self' data: https:` recommended for prod | Backend test asserts photo_url length cap; frontend never executes URL contents |
| Tampering | n/a | n/a | n/a |
| Repudiation | n/a | n/a | n/a |
| Information disclosure | photo_url could be a URL containing PII (e.g., S3 signed URL with embedded user ID) | Phase 1 acceptance: photo_url null. Phase 2 plan editor sanitizes user-provided URLs before persistence | This plan ships with photo_url=null in production seeds |
| Denial of service | Large image could slow /today render | Lazy-load with `loading="lazy"` + width/height attrs; future: Cloudflare image resizing in Phase 2 | Frontend MealCard sets loading="lazy" + explicit dimensions |
| Elevation of privilege | n/a | n/a | n/a |

---
*Plan 01-09 — Phase 1 closure: Lifesum Pure theme propagation. Last updated 2026-05-02.*
